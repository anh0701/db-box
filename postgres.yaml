services:
  postgres:
    image: postgres
    restart: always
    env_file:
      - ./.env
    ports:
      - 5432:5432
    volumes:
      - postgres-vol:/usr/local/pgsql/data

volumes:
  postgres-vol:
    external: true
